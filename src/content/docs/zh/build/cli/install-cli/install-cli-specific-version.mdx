---
title: "安装特定版本的 Aptos CLI（高级）"
description: "面向特殊开发需求和自定义架构的高级指南：从源码构建并安装特定版本的 Aptos CLI。"
sidebar:
  label: "安装自定义版本"
---

import {Aside, Steps} from '@astrojs/starlight/components';

# 使用已有发行版本

## 使用 Aptos CLI 安装脚本

如果已使用 macOS 或 Linux 安装脚本，可以通过以下命令安装指定版本的 Aptos CLI：

```sh
curl -fsSL "https://aptos.dev/scripts/install_cli.sh" | sh -s -- --cli-version 7.9.0
```

## 使用 asdf

如果已使用 [asdf](https://asdf-vm.com/)，可以通过以下命令安装指定版本的 Aptos CLI：

```sh
asdf install aptos 7.9.0
```

## 使用 mise

如果已使用 [mise](https://mise.jdx.dev)，可以通过以下命令安装指定版本的 Aptos CLI：

```sh
mise install aptos@7.9.0
```

# 从源码安装

<Aside title="注意">
  本指南面向需要构建和安装特定版本 Aptos CLI 的高级用户。

  如果只是要安装特定版本的 Aptos CLI，建议使用 ASDF 或 Mise 代为管理。
  可在[此处](#使用已有发行版本)查看安装说明。
</Aside>

如果需要指定版本的 Aptos CLI，可以直接从 Aptos 源码构建。该安装方式主要用于与 Devnet 上尚未进入 Testnet 或 Mainnet 的特定功能交互。如果现有发行版本或预编译二进制文件无法很好地适配你的系统架构，也可以按照这些步骤操作。

如果不需要这种高级安装方式，请参阅[常规安装步骤](/zh/build/cli)。

## 在 macOS / Linux 上安装

<Steps>
  1. 检出 Aptos 源码。

     ```sh
     git clone https://github.com/aptos-labs/aptos-core.git
     ```

  2. 确保已安装 [cargo](https://doc.rust-lang.org/cargo/)。

  3. 构建 Aptos CLI：

     ```sh
     cargo build --package aptos --profile cli
     ```

     二进制文件位于 `target/cli/aptos`。

  4. （可选）将该可执行文件移动到 PATH 中的目录。

  5. 验证安装是否成功。

     ```sh
     target/cli/aptos help
     ```

     这些帮助信息也是特定命令的实用详细指南。
</Steps>

## 在 Windows 上安装

<Steps>
  1. 检出 Aptos 源码。

     ```powershell
     git clone https://github.com/aptos-labs/aptos-core.git
     ```

  2. 确保已安装 [cargo](https://doc.rust-lang.org/cargo/)。

  3. 构建 Aptos CLI。

     ```powershell
     cargo build --package aptos --profile cli
     ```

     二进制文件位于 `target\cli\aptos.exe`。

  4. （可选）将该可执行文件移动到 PATH 中的目录。

  5. 验证安装是否成功。

     ```powershell
     target\cli\aptos.exe help
     ```

     这些帮助信息也是特定命令的实用详细指南。
</Steps>
