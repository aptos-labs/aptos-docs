---
title: "统计质押池的活跃委托人数量"
description: "使用 GraphQL API 查询质押池的活跃委托人数量，以分析委托池参与情况"
sidebar:
  label: "委托人数量"
---

import { GraphQLEditor } from '~/components/react/GraphQLEditor';

import { Aside } from '@astrojs/starlight/components';

`getNumberOfDelegators` 会获取指定池地址的活跃委托人数量。该查询对于需要分析质押池或委托池参与情况的平台特别有价值。

<Aside type="note">
  立即试用！在编辑器中自定义下方变量，探索不同的池地址。
</Aside>

<GraphQLEditor
  query={`query getNumberOfDelegators($poolAddress: String) {
  num_active_delegator_per_pool(
    where: {
      pool_address: { _eq: $poolAddress }
      num_active_delegator: { _gt: "0" }
    }
    distinct_on: pool_address
  ) {
    num_active_delegator
  }
}`}
  variables={`{
  "poolAddress": "0x06099edbe54f242bad50020dfd67646b1e46282999483e7064e70f02f7ea3c15"
}`}
/>

#### 变量：

- `$poolAddress`：**String** — 要查询活跃委托人数量的池地址。例如：`"0x06099edbe54f242bad50020dfd67646b1e46282999483e7064e70f02f7ea3c15"`。
